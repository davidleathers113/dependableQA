[build]
  publish = "dist"
  command = "npm run ci:verify"

[functions]
  node_bundler = "esbuild"

[functions."ai-dispatch-scheduled"]
  schedule = "*/2 * * * *"

[functions."ringba-api-sync-scheduled"]
  schedule = "*/5 * * * *"

[template]
  required-extensions = ["supabase"]