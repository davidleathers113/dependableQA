[build]
  publish = "dist"
  command = "npm run ci:verify"

[functions]
  node_bundler = "esbuild"

[functions."ai-dispatch-scheduled"]
  schedule = "*/2 * * * *"

[template]
  required-extensions = ["supabase"]